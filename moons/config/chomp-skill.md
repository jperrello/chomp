## chomp-skill

Skill template for /chomp. Accepts a git URL or `local`. Runs the chomp bash script to generate the markdown dump, loads it into the RLM REPL, scouts and chunks the context, asks the user what they want to know, then runs a full subcall loop with synthesis.

**Source:** `.claude/skills/chomp-init/templates/chomp-skill.md`
